{
  "buildCommand": "npm install && cd backend && npm install && cd ../frontend && npm install && npm run build && cd .. && npx esbuild backend/src/index.ts --bundle --platform=node --format=cjs --outfile=backend/dist/index.js --external:@prisma/client --external:@supabase/supabase-js",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api", "destination": "/api/index" },
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
